#!/usr/bin/env python3
import sys
from pathlib import Path

for path in (
    Path.home() / ".local" / "share" / "hookbus-normaliser",
    Path("/home/admin/projects/core/hookbus/hookbus/publishers/hookbus-normaliser"),
):
    text = str(path)
    if path.exists() and text not in sys.path:
        sys.path.insert(0, text)

from hookbus_normaliser.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
